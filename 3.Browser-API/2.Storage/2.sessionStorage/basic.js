/* 
Session storage is fully same to local storage.
But the only difference is: it expires after refreshing
the page or visitng to another tab.
*/

/* 
1. get data from local storage: window.sessionStorage.getItem('cart');
2. set data to local storage: window.sessionStorage.setItem('cart', cart);
3. remove data from local storage: window.sessionStorage.removeItem(cart);
*/
