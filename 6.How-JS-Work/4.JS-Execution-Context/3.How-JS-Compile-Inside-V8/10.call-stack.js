/* 
We already know global execution context is an object, and function execution context 
is also an object. But JS is a single threaded language. That means it keeps all objects
in one place one over one. The place where JS keeps all its object is called 
execution stack/call stack.
*/