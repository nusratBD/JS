/* Value will be pushed at the last postion. See the output. */
let vowels = ['a', 'e', 'i', 'o', 'u'];
const push = vowels.push('X');
console.log(vowels);
//output: [ 'a', 'e', 'i', 'o', 'u', 'X' ]
console.log(push);
//output: 6