/* unshift() pushes the new item on the beginning. */
let vowels = ['a', 'e', 'i', 'o', 'u'];
const unshift = vowels.unshift('A');
console.log(vowels);
//output: [ 'A', 'a', 'e', 'i', 'o', 'u' ]
console.log(unshift);
//output: 6